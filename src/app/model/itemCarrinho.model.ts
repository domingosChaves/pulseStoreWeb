export interface ItemCarrinho {
  
    produtoId: number;          // ID único do item no carrinho
    nomeProduto: String;  // ID do carrinho associado
    precoProduto: number;   // ID do produto associado
    quantidade: number;   // Quantidade do produto
  }